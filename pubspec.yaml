name: doleances
description: Gère des doléances dans une liste partagée sur Firebase
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.7
  firebase_core: ^1.1.0
  firebase_auth: ^3.1.0
  cloud_firestore: ^2.5.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^6.0.0
flutter:
  uses-material-design: true
#  assets:
#  - assets/json/jobList.json
#  - assets/images/
